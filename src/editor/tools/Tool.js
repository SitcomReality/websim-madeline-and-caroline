export default class Tool {
    constructor(editorManager) {
        this.editorManager = editorManager;
    }

    onMouseDown(x, y) {}
    onMouseMove(x, y) {}
    onMouseUp(x, y) {}
    onClick(x, y) {}
    update(deltaTime) {}
    draw(ctx) {}
}

